[package]
name = "egui-swift"
version.workspace = true
edition.workspace = true
description = "A generic SwiftUI-style UI framework built on top of egui"

[dependencies]
egui = "0.31"

[dev-dependencies]
eframe = { version = "0.31", default-features = false, features = ["default_fonts", "glow"] }
